pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'fireblocks-external-key-store'

include 'fireblocks-application'
include 'fireblocks-service'
include 'fireblocks-datamodel'
include 'fireblocks-docker'